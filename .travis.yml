language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t jerrymajewski/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true jerrymajewski/docker-react npm run test
